For Client :::>>side You have to start the Client side by using command (npm start)
For Server Side ::> 1>We have to start the yarn by the command (start yarn) in a different integreted file
::> 2 We have to again open the integreted file and run the command of npm start/
------------------------------------------------------------------------------------------------------------
Don't you dare to run the database it get automatically with the server when you started the server with the above command

